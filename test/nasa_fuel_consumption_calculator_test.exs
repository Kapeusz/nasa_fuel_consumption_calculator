defmodule NasaFuelConsumptionCalculatorTest do
  use ExUnit.Case
  doctest NasaFuelConsumptionCalculator

  test "greets the world" do
    assert NasaFuelConsumptionCalculator.hello() == :world
  end
end
